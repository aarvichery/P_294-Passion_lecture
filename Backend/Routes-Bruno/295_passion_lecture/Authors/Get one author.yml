info:
  name: Get one author
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3333/v1/api/authors/:id
  params:
    - name: id
      value: "1"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
