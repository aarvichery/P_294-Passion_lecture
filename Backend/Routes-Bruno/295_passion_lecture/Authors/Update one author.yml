info:
  name: Update one author
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3333/v1/api/authors/:id
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Veya1",
        "firstname": "Elias1"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
