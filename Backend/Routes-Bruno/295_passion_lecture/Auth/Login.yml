info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3333/v1/api/users/login
  body:
    type: json
    data: |-
      {
              "email": "aaron.vichery1@eduvaud.ch",
              "password": "lkasvbifbsizv451"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
