info:
  name: Delete one comment
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:3333/v1/api/books/:book_id/comments/:id
  params:
    - name: book_id
      value: "1"
      type: path
    - name: id
      value: "1"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
