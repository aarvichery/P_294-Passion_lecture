info:
  name: Get all books
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3333/v1/api/books
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
