info:
  name: Get one categorie
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3333/v1/api/categories/:id
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
